app:
  name: wsnotifier
  shutdown_timeout_sec: 5
  web:
    port: 8002
    read_timeout_sec: 600
    write_timeout_sec: 600
  token:
    secret: secret
    expire_sec: 43200

log:
  level: info
  format: json

nats:
  host: localhost
  port: 4222
